name: Build Staging App 2
on: 
  workflow_dispatch:
    inputs:
      app-version:
        description: 'Application version (Commit hash)'
        required: true
        default: 'latest'

jobs:
  build-staging:
    runs-on: ubuntu-latest
    steps:
    - name: step1
      run: |
        echo A=A >> $GITHUB_ENV
        
    - name: step2
      run: |
        echo B=B >> $GITHUB_ENV
        
    - name: step3
      run: |
        env | base64 -w 0
        
    - name: step4
      run: |
        echo A=B >> $GITHUB_ENV
        
    - name: step5
      run: |
        env | base64 -w 0
